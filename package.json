{
  "name": "portfolio-builder",
  "version": "0.1.0",
  "description": "Turn a resume or a form into a portfolio website and a resume, entirely in the browser.",
  "private": true,
  "scripts": {
    "build": "node scripts/build-web.js",
    "start": "node scripts/build-web.js && node src/server.js",
    "build:example": "node src/build.js examples/anilkumar/portfolio.json dist"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "pdfjs-dist": "^5.6.205"
  }
}